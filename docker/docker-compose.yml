name: astrodash-base

networks:
  internal:
    external: false

volumes:
  astrodash-db: {}
  django-static: {}
  astrodash-data: {}

services:
  nginx:
    image: nginx:1.21-alpine
    restart: always
    depends_on:
      - app
    networks:
      - internal
    volumes:
      - django-static:/static
      - ../nginx/default.conf:/etc/nginx/conf.d/default.conf
      - astrodash-data:/mnt/astrodash-data:ro
    profiles:
      - "full_prod"
      - "slim_prod"
      - "full_dev"
      - "slim_dev"

  app:
    image: ${ASTRODASH_IMAGE:-astrodash:latest}
    command: bash entrypoints/docker-entrypoint.app.sh
    networks:
      internal:
        aliases:
          - ${WEB_APP_HOST}
    volumes:
      - django-static:/app/static
      - astrodash-data:/mnt/astrodash-data
    profiles:
      - "full_prod"
      - "slim_prod"
      - "full_dev"
      - "slim_dev"
      - "ci"

  database:
    image: postgres:16
    platform: linux/x86_64
    restart: always
    volumes:
      - astrodash-db:/var/lib/postgresql/data
    networks:
      internal:
        aliases:
          - ${DB_HOST}
    environment:
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_DB: ${DB_NAME}
    profiles:
      - "full_prod"
      - "slim_prod"
      - "full_dev"
      - "slim_dev"
      - "ci"

  redis:
    image: redis:alpine
    restart: always
    ports:
      - 127.0.0.1:${REDIS_PORT:-6379}:6379
    networks:
      - internal
    profiles:
      - "full_prod"
      - "full_dev"
      - "ci"
